/*
 * FeedsPage Messages
 *
 * This contains all the text for the FeedsPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.FeedsPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FeedsPage container!',
  },
});
