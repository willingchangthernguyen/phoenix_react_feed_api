/*
 * AddFeedPage Messages
 *
 * This contains all the text for the AddFeedPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddFeedPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddFeedPage container!',
  },
});
